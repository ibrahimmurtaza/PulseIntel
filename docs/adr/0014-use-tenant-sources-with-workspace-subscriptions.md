# Use tenant-owned sources with workspace subscriptions

PulseIntel will define sources once at the tenant level, then let each workspace attach to them through a workspace-level subscription/configuration. We chose this because teams in the same tenant should be able to reuse source setup without being forced into identical dashboard behavior or source usage.
