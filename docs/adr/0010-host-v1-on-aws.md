# Host v1 on AWS

PulseIntel will target AWS as its primary hosting platform for the first release. We chose AWS because it fits the application's mix of web services, background workers, managed Postgres, Redis, object storage, and email-adjacent infrastructure without forcing unusual platform workarounds early on.
