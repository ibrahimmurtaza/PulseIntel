{
  "name": "pulseintel",
  "private": true,
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace @pulseintel/web",
    "typecheck": "npm run typecheck --workspace @pulseintel/web",
    "test": "npm run test --workspace @pulseintel/web"
  }
}
